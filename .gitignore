# Dados reais do haras — nunca vão pro repositório público. O app guarda tudo no navegador
# (localStorage); esses arquivos são só backups locais que você exporta de vez em quando.
haras_backup_*.json

# Pastas de sessão/config do Claude Code, não fazem parte do app.
.claude/

# Referência de antes/depois da última mudança visual — nunca commitar, são só pra comparar/reverter.
# Convenção: sempre que eu mudar o app_145.html de forma visível, guardo a versão anterior como
# "Original" e a versão nova como "Novo" (sobrescrevendo o par anterior).
app_145_original.html
app_145_novo.html
